<section class="cart-show">

  <header class="page-header">
    <h1>My Cart</h1>
  </header>

  <div class="panel panel-default items">
    <% if cart.empty? %>
    <%= render 'empty' %>
    <% else %>
    <%= render 'with_items' %>
    <% end %>

</section>
